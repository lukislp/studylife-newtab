{
  "name": "studylife-newtab",
  "version": "1.1.3",
  "private": true,
  "description": "Chrome extension: replaces the New Tab page with a StudyLife stats dashboard",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "package": "node zip.mjs",
    "contract-check": "node scripts/contract-check.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/archiver": "^8.0.0",
    "@types/chrome": "^0.3.0",
    "archiver": "^8.0.0",
    "esbuild": "^0.28.0",
    "fast-check": "4.10.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
